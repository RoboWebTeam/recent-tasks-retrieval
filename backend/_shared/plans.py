# Единый источник месячных лимитов единиц по тарифам. Импортируется generate-site (списание+сброс),
# auth (месячный сброс) и yookassa-webhook (выставление лимита ПОСЛЕ ОПЛАТЫ) — чтобы значения
# НИКОГДА не расходились между функциями. Раньше PLAN_LIMITS был захардкожен в 3 местах и две копии
# отставали → оплатившие Premium получали не тот лимит. Единица = 1 обычная генерация на Sonnet 5;
# Opus и крупные задачи списывают больше (см. MODEL_UNIT_COST в generate-site).
#
# Калибровка (net-выручка за единицу ≥29₽ выше себестоимости): значения для pro-* совпадают с
# колонкой requests в таблице plan_pricing; при смене цен менять ЗДЕСЬ и в plan_pricing синхронно.
PLAN_LIMITS = {
    'free': 10,
    'premium': 30,
    'pro_60': 60,
    'pro_80': 80,
    'pro_200': 160,
    'pro_400': 320,
    'pro_800': 660,
}
